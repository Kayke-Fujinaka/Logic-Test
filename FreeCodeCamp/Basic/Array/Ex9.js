// Manipulate Arrays With shift()

const ourArray = ["Stimpson", "J", ["cat"]];
// Vai remover o primeiro elemento do array 'ourArray'
const removedFromOurArray = ourArray.shift();
console.log(ourArray)